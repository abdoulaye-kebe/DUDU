# Copier vers `.env` sur le serveur et remplir les valeurs (ne pas commiter `.env`).

# Wave — portail développeur (API Keys + secret du webhook SIGNING_SECRET)
WAVE_MODE=production
WAVE_API_KEY=
WAVE_WEBHOOK_SECRET=
WAVE_CALLBACK_URL=https://www.dudugroup.sn/api/v1/mobile-payments/wave/webhook
WAVE_BUSINESS_PHONE=
